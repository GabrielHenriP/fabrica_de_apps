import React, { useContext } from 'react';
import { Button, Text, View } from 'react-native';

export default New = () => {

    return(
        <View>
            <Text>New</Text>
        </View>
    )
};